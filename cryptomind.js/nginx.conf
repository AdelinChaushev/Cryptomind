server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # This is critical for React Router - without it, refreshing any page
    # other than / will return a 404 from Nginx
    location / {
        try_files $uri $uri/ /index.html;
    }
}
